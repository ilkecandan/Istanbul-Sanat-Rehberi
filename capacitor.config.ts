import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'istanbul.sanat.rehberi',
  appName: 'ARTINGEN',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
